# Database (Postgres). For local dev use the bundled docker-compose service.
DATABASE_URL=postgres://postgres:postgres@127.0.0.1:5432/upb_rares

# Payload — generate with: openssl rand -hex 32
PAYLOAD_SECRET=

# Public
NEXT_PUBLIC_SITE_URL=http://localhost:3000
